module.exports = function(settings) {

  exports.js =
    "var v1 = 1000;\
var url = '" + settings.url +  "/test/wait/' + v1/2;\
var r = y.rest(url).timeout(v1).get().response;\
response.object = save(r, v1);";

  return exports;
}
